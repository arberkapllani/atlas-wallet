[package]
name = "atlas-settings"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/arberkapllani/atlas-wallet"
description = "Persistent user settings for Atlas (RPC overrides, preferences)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
tempfile = "3.10"
