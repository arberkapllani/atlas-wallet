{
  "name": "atlas",
  "version": "0.1.0",
  "private": true,
  "description": "Atlas — sovereign multi-chain desktop wallet",
  "scripts": {
    "dev": "pnpm --filter atlas-desktop tauri dev",
    "build": "pnpm --filter atlas-desktop tauri build",
    "check": "pnpm --filter atlas-desktop check",
    "lint": "pnpm --filter atlas-desktop lint"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@9.12.0"
}

